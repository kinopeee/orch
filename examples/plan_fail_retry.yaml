goal: "failure and retry flow"
tasks:
  - id: unstable
    cmd: ["python", "tools/fake_agent.py", "build", "--fail-always"]
    retries: 1
    retry_backoff_sec: [0.1]

  - id: downstream
    cmd: ["python", "tools/fake_agent.py", "test"]
    depends_on: ["unstable"]
